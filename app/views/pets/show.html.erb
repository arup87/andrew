<h1><%= @pet.name%></h1>
<p> <%=@pet.type_of_pet%></p>
<% @pet.appointments.each do |appointment| %>

<p><%=appointment.date_of_visit%></p>
<%end%>
<%= link_to "<< All Pets", pets_path%> || <%= link_to "Edit Pet", edit_pet_path(@pet) %> 

